node {
  //agent any
//  def app
  stages {
    stage('Clone repo') {
  //    checkout scm
  //  sh 'echo "Clone"'
    }
    stage ('Build stage'){
      //steps {
        //app = docker.build(".")
    //      sh 'echo "Build"'
    //  }
    }
    stage('Testing Stage') {
      //sh 'echo "Testing"'
      //app.inside{
        //sh 'echo "Test passed"'
      //}
    }
    stage('Push image') {
      //sh 'echo "Push Image"'
      //docker.writeRegistry('https://registry.hub.docker.com','docker-hub-credentials'){
        //  app.push("${env.BUILD_NUMBER}")
          //app.push("latest")
      }

    stage('Deploy stage') {
    //  sh 'echo "Deploy"'
    }
  }
  }
